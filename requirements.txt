# Reproducibility lock-file for development / CI.
# The package metadata lives in pyproject.toml (single source of truth);
# this file pins specific versions known to work end-to-end.

tensorflow==2.10.0
tensorflow-probability==0.18.0
scikit-learn>=1.2.1
numpy
h5py
pyyaml
